<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guihang.medicalbackend.mapper.PermissionMapper">
    <!--查询所有菜单的map映射-->
    <resultMap id="selectMap" type="com.guihang.medicalbackend.pojo.Permission">
        <id property="id" column="id"/>
        <result property="pid" column="pid"/>
        <result property="name" column="name"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="level" column="level"/>
        <result property="title" column="title"/>
    </resultMap>

<!--    <select id="getPermissionByRoleName" resultMap="selectMap">-->
<!--        # 多表关联查询 内连接（左内连接， 右内连接）-->
<!--        select p.*-->
<!--        from role_permission rp-->
<!--                 left join permission p-->
<!--                           on rp.per_id = p.id-->
<!--        where roleName = #{roleName}-->

<!--        # 多表关联查询 等值连接-->
<!--        select p.* from role_permission rp ,permission p-->
<!--        where rp.per_id = p.id and rp.roleName=#{roleName}-->
<!--        </select>-->

    <select id="getPermissionByRoleName" resultMap="selectMap">
        select p.* from role_permission rp ,permission p
        where rp.per_id = p.id and rp.roleName=#{roleName}
    </select>
</mapper>